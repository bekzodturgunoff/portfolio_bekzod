---
const { id, title, children } = Astro.props;
---
<section id={id} class="max-w-6xl mx-auto px-4 py-20" data-reveal>
  <h2 class="text-2xl md:text-3xl font-semibold tracking-tight mb-8 text-neutral-900 dark:text-neutral-100">{title}</h2>
  <div>
    <slot />
  </div>
</section>
